export const metadata = {
  title: "Fintech App Development | Systrocode",
  description: "Build secure financial apps, payment gateways, and banking solutions. PCI-DSS compliant with $2B+ transactions processed across 100+ fintech apps.",
};

export default function FintechLayout({ children }) {
  return children;
}
